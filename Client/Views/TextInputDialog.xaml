<ui:FluentWindow x:Class="Voiceover.Client.Views.TextInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Voiceover" Height="180" Width="360"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgDark}"
        ResizeMode="NoResize"
        Icon="/Assets/AppIcon.png"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar x:Name="DialogTitleBar" Title="Voiceover" Grid.Row="0" ShowMaximize="False"/>

        <StackPanel Grid.Row="1" Margin="20">
            <TextBlock x:Name="LabelText" FontWeight="Bold"
                       Foreground="{StaticResource TextNormal}" Margin="0,0,0,8"/>
            <TextBox x:Name="InputBox" Height="36" Padding="8,6"
                     Background="{StaticResource BgDarker}" Foreground="{StaticResource TextNormal}"
                     BorderThickness="0" Margin="0,0,0,16" KeyDown="InputBox_KeyDown"/>
            <Button x:Name="OkButton" Content="OK" Height="40"
                    Background="{StaticResource AccentBlurple}" Foreground="White"
                    BorderThickness="0" FontWeight="Bold" Cursor="Hand" Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
