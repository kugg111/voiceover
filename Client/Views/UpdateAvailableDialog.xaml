<ui:FluentWindow x:Class="Voiceover.Client.Views.UpdateAvailableDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Update Available" Height="260" Width="380"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgDark}"
        ResizeMode="NoResize"
        Icon="/Assets/AppIcon.png"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Title="Update Available" Grid.Row="0" ShowMaximize="False"/>

        <StackPanel Grid.Row="1" Margin="20">
            <TextBlock x:Name="VersionText" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"
                       Foreground="{StaticResource TextNormal}" Margin="0,0,0,8"/>
            <TextBlock x:Name="DetailText" FontSize="12" TextWrapping="Wrap"
                       Foreground="{StaticResource TextMuted}" Margin="0,0,0,20"/>

            <Button x:Name="DownloadButton" Content="Download Update" Height="40" Margin="0,0,0,10"
                    Click="DownloadButton_Click" FontWeight="Bold"
                    Background="{StaticResource AccentBlurple}" Foreground="White" BorderThickness="0"/>
            <Button x:Name="DismissButton" Content="Not Now" Height="36"
                    Click="DismissButton_Click"
                    Background="Transparent" Foreground="{StaticResource TextMuted}" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
