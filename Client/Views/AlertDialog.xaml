<ui:FluentWindow x:Class="Voiceover.Client.Views.AlertDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Voiceover" Width="360" SizeToContent="Height" MinHeight="0"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgDark}"
        ResizeMode="NoResize"
        Icon="/Assets/AppIcon.png"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar x:Name="DialogTitleBar" Title="Voiceover" Grid.Row="0" ShowMaximize="False"/>

        <StackPanel Grid.Row="1" Margin="20">
            <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="14"
                       Foreground="{StaticResource TextNormal}" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="OkButton" Content="OK" Height="36" Width="90"
                        Background="{StaticResource AccentBlurple}" Foreground="White" FontWeight="Bold"
                        BorderThickness="0" Cursor="Hand" Click="OkButton_Click" IsDefault="True" IsCancel="True"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
